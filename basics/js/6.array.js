let one= 'one';
let two = 'two';

let heros = ['Iron man', 'Thor','Flash','spider Man','Super Man'];

console.log(heros[0]);
console.log(heros.length);

console.log('we have '+ heros.length + ' superHeros');
console.log(`We have ${heros.length} SuperHeros`);